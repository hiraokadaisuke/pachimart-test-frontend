import { getSalesSlip } from '@/features/inventory/sales-slips';
export default async function Page({params}:{params:Promise<{id:string;type:string}>}){const {id,type}=await params;const s=await getSalesSlip(id);return <div style={{maxWidth:800,margin:'0 auto',padding:12,fontSize:12}}><style>{'@media print { body { margin: 0; } } table{border-collapse:collapse;width:100%} td,th{border:1px solid #333;padding:4px;}'}</style><h1>{type}</h1><p>販売先: {s?.customerName}</p><table><thead><tr><th>機種</th><th>数量</th><th>金額</th></tr></thead><tbody>{s?.lines.map(l=><tr key={l.id}><td>{l.machineName}</td><td>{l.quantity}</td><td>{l.amount}</td></tr>)}</tbody></table></div>}
